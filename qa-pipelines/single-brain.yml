# Concourse pipeline to run a single brain test

resources:
- name: ci
  type: git
  source:
    uri: ((src-ci-repo))
    branch: ((src-ci-branch))
    paths:
    - qa-pipelines/*
    - sample-apps/*

- name: s3.scf-config-sles
  type: s3
  source:
    endpoint: ((s3-config-endpoint))
    access_key_id: ((s3-config-access-key))
    secret_access_key: ((s3-config-secret-key))
    bucket: ((s3-config-bucket-sles))
    regexp: ((s3-config-prefix-sles))scf-sle-(.*)\.zip$

- name: pool.kube-hosts
  type: pool
  source:
    uri: ((kube-pool-repo))
    private_key: ((kube-pool-key))
    branch: ((kube-pool-branch))
    pool: ((kube-pool-pool))
jobs:
- name: ((pipeline-name))-001
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "001"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-002
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "002"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-003
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "003"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-004
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "004"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-005
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "005"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-006
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "006"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-007
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "007"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-008
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "008"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-009
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "009"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-010
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "010"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-011
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "011"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-012
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "012"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-013
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "013"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-014
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "014"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-015
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "015"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-016
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "016"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-017
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "017"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
- name: ((pipeline-name))-018
  plan:
  - do:
    - aggregate:
      - get: ci
      - get: s3.scf-config-sles
        trigger: true
      - put: pool.kube-hosts
        params: {acquire: true}
    on_failure:
      put: pool.kube-hosts
      params: {release: pool.kube-hosts}
  - do:
    - task: acceptance-tests-brain
      file: ci/qa-pipelines/tasks/run-test.yml
      params:
        ENABLE_CF_BRAIN_TESTS: true
        INCLUDE_BRAINS_PREFIX: "018"
      input_mapping:
        s3.scf-config: s3.scf-config-sles
    - put: pool.kube-hosts
      params: {release: pool.kube-hosts}
